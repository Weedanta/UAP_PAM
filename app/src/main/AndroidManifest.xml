<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/tudulogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/tudulogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDoList"
        tools:targetApi="31">
<<<<<<< HEAD
=======

        <!-- Login Activity - Entry point -->
>>>>>>> b61f967 (final commit)
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<<<<<<< HEAD
        <activity
            android:name=".AddTodoActivity"
            android:exported="false" />
        <activity
            android:name=".EditTodoActivity"
            android:exported="false" />
=======

        <!-- Register Activity -->
        <activity
            android:name=".ui.main.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.ToDoList.NoActionBar" />

        <!-- Main Activity -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.ToDoList.NoActionBar" />

        <!-- Add Todo Activity - FIX: Tambahkan NoActionBar theme -->
        <activity
            android:name=".ui.main.AddTodoActivity"
            android:exported="false"
            android:theme="@style/Theme.ToDoList.NoActionBar"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Edit Todo Activity - FIX: Tambahkan NoActionBar theme -->
        <activity
            android:name=".ui.main.EditTodoActivity"
            android:exported="false"
            android:theme="@style/Theme.ToDoList.NoActionBar"
            android:parentActivityName=".ui.main.MainActivity" />



>>>>>>> b61f967 (final commit)
    </application>

</manifest>